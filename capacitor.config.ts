import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.nextjscapacitorapp',
  appName: 'Next.js Capacitor App',
  webDir: 'out',
  plugins: {
    // Add any required plugins here
  },
};

export default config;